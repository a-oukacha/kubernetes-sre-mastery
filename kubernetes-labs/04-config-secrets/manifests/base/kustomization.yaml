apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - configmap.yaml
  - secret.yaml
  - deployment.yaml

# Common labels applied to every object the overlays render.
labels:
  - includeSelectors: false
    pairs:
      app.kubernetes.io/part-of: k8s-sre-course
      course.lab: "04"
