# Same pod, but the image tag does not exist. Used in "Break it".
apiVersion: v1
kind: Pod
metadata:
  name: echo-broken
  labels:
    app.kubernetes.io/name: echo
    app.kubernetes.io/part-of: k8s-sre-course
    course.lab: "01"
    app: demo
    color: red
spec:
  containers:
    - name: echo
      image: registry.k8s.io/e2e-test-images/agnhost:2.47-THIS-TAG-DOES-NOT-EXIST
      args: ["netexec", "--http-port=8080"]
      resources:
        requests:
          cpu: 25m
          memory: 32Mi
